spring.application.name=demo
# ===============================================================
# Neon PostgreSQL Connection - PLEASE REPLACE THE PLACEHOLDERS
# ===============================================================
# Replace <HOST>, <DATABASE>, <USER>, and <PASSWORD> with your Neon credentials.
spring.datasource.url=jdbc:postgresql://ep-green-lake-ac8xkgyc-pooler.sa-east-1.aws.neon.tech:5432/ecocycle?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_peN31SjtzZXT
spring.datasource.driver-class-name=org.postgresql.Driver
# This setting prevents Hibernate from automatically creating/altering tables.
# It's set to 'none' because you have already created the tables manually.
spring.jpa.hibernate.ddl-auto=none

# This shows the SQL queries generated by Hibernate in the console.
spring.jpa.show-sql=true

# This setting ensures that database initialization scripts are always run.
spring.sql.init.mode=always


server.port=8081